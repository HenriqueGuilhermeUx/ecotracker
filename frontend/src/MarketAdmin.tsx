import { FormEvent,useEffect,useState } from "react";
import { api } from "./api";
import { MarketShell } from "./MarketApp";
import { Asset,dateTime,money,num,Quote } from "./market-types";

export function MarketAdmin(){const[token,setToken]=useState(localStorage.getItem("ecotracker_admin_token"));if(!token)return <Login onLogin={t=>{localStorage.setItem("ecotracker_admin_token",t);setToken(t)}}/>;return <Panel logout={()=>{localStorage.removeItem("ecotracker_admin_token");setToken(null)}}/>}
function Login({onLogin}:{onLogin:(t:string)=>void}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();try{const r=await api<{token:string}>("/auth/login",{method:"POST",body:JSON.stringify({email,password})});onLogin(r.token)}catch(e){setMessage((e as Error).message)}}return <MarketShell><main className="market-admin-login"><form onSubmit={submit}><span className="tag">OPERAÇÃO COMERCIAL</span><h1>EcoRouter Admin</h1><input required type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input required type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)}/><button>Entrar</button>{message&&<div className="form-msg">{message}</div>}</form></main></MarketShell>}
function Panel({logout}:{logout:()=>void}){const[tab,setTab]=useState<"quotes"|"assets">("quotes"),[assets,setAssets]=useState<Asset[]>([]),[quotes,setQuotes]=useState<Quote[]>([]),[message,setMessage]=useState(""),[refreshing,setRefreshing]=useState(false);async function load(){try{const[a,q]=await Promise.all([api<Asset[]>("/admin/market/assets"),api<Quote[]>("/admin/market/quotes")]);setAssets(a);setQuotes(q)}catch(e){setMessage((e as Error).message)}}useEffect(()=>{load()},[]);async function refresh(){setRefreshing(true);try{await api("/admin/market/refresh",{method:"POST"});await load();setMessage("Fontes e câmbio atualizados.")}catch(e){setMessage((e as Error).message)}finally{setRefreshing(false)}}return <MarketShell><main className="market-admin-page"><div className="market-admin-head"><div><span className="tag">ECOROUTER</span><h1>Operação comercial</h1></div><div><button className="secondary-admin" onClick={refresh}>{refreshing?"Atualizando...":"Atualizar fontes"}</button><button className="secondary-admin" onClick={logout}>Sair</button></div></div><div className="market-admin-tabs"><button className={tab==="quotes"?"active":""} onClick={()=>setTab("quotes")}>Cotações ({quotes.length})</button><button className={tab==="assets"?"active":""} onClick={()=>setTab("assets")}>Ativos ({assets.length})</button><a href="#admin">Admin de lastro →</a></div>{message&&<div className="notice">{message}</div>}{tab==="quotes"?<Quotes quotes={quotes} reload={load}/>:<Assets assets={assets} reload={load}/>}</main></MarketShell>}
function Quotes({quotes,reload}:{quotes:Quote[];reload:()=>void}){async function save(q:Quote,status:string,final:string,notes:string){await api(`/admin/market/quotes/${q.id}`,{method:"PATCH",body:JSON.stringify({status,finalTotal:final?Number(final):null,adminNotes:notes})});reload()}return <section className="quote-admin-list">{quotes.map(q=><QuoteCard key={q.id} quote={q} save={save}/>)}{!quotes.length&&<div className="empty">Nenhuma solicitação ainda.</div>}</section>}
function QuoteCard({quote,save}:{quote:Quote;save:(q:Quote,s:string,f:string,n:string)=>Promise<void>}){const[status,setStatus]=useState(quote.status),[final,setFinal]=useState(quote.final_total||""),[notes,setNotes]=useState(quote.admin_notes||""),[saving,setSaving]=useState(false);return <article className="quote-admin-card"><div className="quote-admin-title"><div><span>{quote.registry}</span><h3>{quote.project_name}</h3><small>{quote.public_code}</small></div><b>{num(quote.requested_kg,0)} kg</b></div><div className="quote-person"><strong>{quote.buyer_name}</strong><span>{quote.buyer_email}{quote.buyer_phone?` · ${quote.buyer_phone}`:""}</span><span>{quote.company_name||"Pessoa física"} · {quote.delivery_mode}</span></div><div className="quote-admin-fields"><select value={status} onChange={e=>setStatus(e.target.value)}>{["requested","reviewing","quoted","awaiting_payment","sourcing","retired","delivered","cancelled"].map(x=><option key={x} value={x}>{x.replaceAll("_"," ")}</option>)}</select><input type="number" min="0" step="0.01" placeholder="Valor final R$" value={final} onChange={e=>setFinal(e.target.value)}/><textarea placeholder="Notas internas" value={notes} onChange={e=>setNotes(e.target.value)}/><button disabled={saving} onClick={async()=>{setSaving(true);try{await save(quote,status,final,notes)}finally{setSaving(false)}}}>{saving?"Salvando...":"Salvar cotação"}</button></div></article>}
function Assets({assets,reload}:{assets:Asset[];reload:()=>void}){return <section><NewAsset reload={reload}/><div className="asset-admin-list">{assets.map(a=><AssetCard key={a.id} asset={a} reload={reload}/>)}</div></section>}
function NewAsset({reload}:{reload:()=>void}){const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget).entries());try{await api("/admin/market/assets",{method:"POST",body:JSON.stringify({...d,sourcePriceUsdTon:d.sourcePriceUsdTon?Number(d.sourcePriceUsdTon):null,minOrderKg:Number(d.minOrderKg||100),fxBrlUsd:Number(d.fxBrlUsd||5.5),serviceMarginPct:Number(d.serviceMarginPct||25),fixedFeeBrl:0,pricingMode:d.sourcePriceUsdTon?"dynamic":"quote",availabilityStatus:"monitoring",sourceStatus:"manual",active:true})});e.currentTarget.reset();setMessage("Fonte adicionada.");reload()}catch(e){setMessage((e as Error).message)}}return <form className="new-asset-form" onSubmit={submit}><h2>Adicionar fonte ou projeto</h2><input name="registry" required placeholder="Registry"/><input name="projectName" required placeholder="Nome comercial"/><input name="sourceReference" required placeholder="Referência única"/><input name="sourceUrl" type="url" placeholder="URL da origem"/><input name="assetType" defaultValue="carbon" placeholder="Tipo"/><input name="qualityTier" defaultValue="screening" placeholder="Tier"/><input name="sourcePriceUsdTon" type="number" min="0" step="0.0001" placeholder="Preço USD/t (opcional)"/><input name="fxBrlUsd" type="number" min="0" step="0.0001" defaultValue="5.5"/><input name="serviceMarginPct" type="number" min="0" step="0.01" defaultValue="25"/><input name="minOrderKg" type="number" min="1" defaultValue="100"/><textarea name="description" placeholder="Descrição"/><button>Adicionar</button>{message&&<div className="form-msg">{message}</div>}</form>}
function AssetCard({asset,reload}:{asset:Asset;reload:()=>void}){const[price,setPrice]=useState(asset.source_price_usd_ton||""),[fx,setFx]=useState(asset.fx_brl_usd),[margin,setMargin]=useState(asset.service_margin_pct),[available,setAvailable]=useState(asset.available_tons||""),[status,setStatus]=useState(asset.availability_status),[active,setActive]=useState(asset.active),[saving,setSaving]=useState(false);async function save(){setSaving(true);try{await api(`/admin/market/assets/${asset.id}`,{method:"PATCH",body:JSON.stringify({sourcePriceUsdTon:price?Number(price):null,fxBrlUsd:Number(fx),serviceMarginPct:Number(margin),availableTons:available?Number(available):null,availabilityStatus:status,pricingMode:price?"dynamic":"quote",active})});reload()}finally{setSaving(false)}}return <article className="asset-admin-card"><div><span>{asset.registry}</span><h3>{asset.project_name}</h3><small>{asset.source_reference} · {asset.source_status} · {dateTime(asset.last_checked_at)}</small></div><div className="asset-admin-fields"><label>USD/t<input type="number" min="0" step="0.0001" value={price} onChange={e=>setPrice(e.target.value)}/></label><label>USD/BRL<input type="number" min="0" step="0.0001" value={fx} onChange={e=>setFx(e.target.value)}/></label><label>Margem %<input type="number" min="0" step="0.01" value={margin} onChange={e=>setMargin(e.target.value)}/></label><label>Disponível t<input type="number" min="0" step="0.000001" value={available} onChange={e=>setAvailable(e.target.value)}/></label><label>Status<select value={status} onChange={e=>setStatus(e.target.value as Asset["availability_status"])}><option value="monitoring">monitoring</option><option value="indicative">indicative</option><option value="confirmed">confirmed</option></select></label><label className="checkbox-label"><input type="checkbox" checked={active} onChange={e=>setActive(e.target.checked)}/> Ativo</label><button disabled={saving} onClick={save}>{saving?"Salvando...":"Salvar"}</button></div><div className="admin-price-preview">Preço público: <b>{asset.indicative_price_brl_kg?`${money(asset.indicative_price_brl_kg)}/kg`:"sob consulta"}</b></div></article>}
